langgraph>=0.2
openai>=1.70          # used as OpenRouter client (compatible API)
sentence-transformers>=3.0  # local embeddings, no API key (all-MiniLM-L6-v2, 384 dims)
httpx>=0.28
feedparser>=6.0
supabase>=2.0
celery[redis]>=5.3
apscheduler>=3.10
python-dotenv>=1.0
bleach>=6.0
resend>=2.0
# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12

