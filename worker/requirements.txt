langgraph==1.1.6
openai==2.31.0          # used as OpenRouter client (compatible API)
sentence-transformers==5.4.1  # local embeddings, no API key (all-MiniLM-L6-v2, 384 dims)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu       # CPU-only — no CUDA needed, saves ~3GB
httpx==0.28.1
feedparser==6.0.12
supabase==2.28.3
celery[redis]==5.6.3
apscheduler==3.11.2
python-dotenv==1.2.2
bleach==6.3.0
resend==2.28.1
defusedxml>=0.7       # protect feedparser against XML bombs from untrusted RSS feeds
sentry-sdk>=2.0       # error tracking and performance monitoring
# Dev / test
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1

